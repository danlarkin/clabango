0.6 ???
* Template filters can now be chained.
* Variables are now html-escaped by default. The `safe` filter can be used to exempt a variable from being escaped.

0.5 2013-03-10
* The `if` and `ifequal` tags now support `else` branches.

0.4 2012-12-04
* Fixed bug with overriding blocks contained in other block overrides.

0.3 2012-11-13
* Initial Release.
